export interface POST {
  title: string;
  subTitle: string;
  image: string;
  body: string;
  id: string;
}

export interface CONTACT {
  firstName: string;
  secondName: string;
  position: string;
  email: string;
  phone: string;
}